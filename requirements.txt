# ── Web server (replaces Qt popup window) ──────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0   # includes websockets support

# ── Core ML ────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0
faiss-cpu>=1.7.4
numpy>=1.24.0
ftfy>=6.1.1
regex>=2023.0.0
tqdm>=4.65.0
timm>=0.9.0
ultralytics>=8.0.0
easyocr>=1.7.0

# ── OpenAI CLIP (install separately) ──────────────────────────────
# pip install git+https://github.com/openai/CLIP.git

# ── Optional: Qt UI (kept for backward compatibility only) ─────────
# PyQt5>=5.15.0
# PyQtWebEngine>=5.15.0

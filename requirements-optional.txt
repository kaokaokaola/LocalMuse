# ============================================================
#  LocalMuse V2 - Optional dependencies (graceful degradation)
#
#  setup.bat installs this file via:
#    venv\Scripts\python.exe -m pip install -r requirements-optional.txt
#
#  If any package here fails to install, setup.bat only prints a
#  warning and continues - LocalMuse still starts and runs, with
#  the matching feature(s) disabled:
#    - ultralytics missing      -> pose search disabled
#    - easyocr missing          -> in-image OCR text search disabled
#    - transformers /
#      multilingual-clip missing -> semantic search stays English-only
#                                   (no Chinese/Japanese/Korean/etc.)
#    - qwen-vl-utils /
#      accelerate / transformers
#      missing                  -> VLM annotation ("Annotate") disabled
#
#  As with requirements.txt, this file is the single source of
#  truth for these version constraints - do not hardcode them
#  again in setup.bat or setup_annotation.bat.
# ============================================================

# --- Pose search (YOLOv8-Pose keypoints) ---
ultralytics>=8.0.0

# --- In-image OCR text search ---
easyocr>=1.7.0

# --- Multilingual semantic search (M-CLIP: Chinese/Japanese/Korean + 50 langs) ---
# transformers>=4.51.0 is also required for Qwen3-VL annotation below.
transformers>=4.51.0
multilingual-clip>=1.0.0

# --- VLM annotation (Qwen3-VL / Qwen2.5-VL image captioning) ---
qwen-vl-utils>=0.0.8
accelerate>=0.26.0
